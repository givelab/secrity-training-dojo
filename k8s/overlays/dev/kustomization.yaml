apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ⚠️ 脆弱な開発環境オーバーレイ（学習目的）

bases:
  - ../../base

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: backend
